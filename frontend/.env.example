# Production Backend API URL
# Leave blank during local development to use Vite's proxy (/api -> http://localhost:8000)
# Set to your deployed backend URL in production (e.g. on Vercel / Netlify environment variables)
VITE_API_URL=https://your-markme-backend.onrender.com
